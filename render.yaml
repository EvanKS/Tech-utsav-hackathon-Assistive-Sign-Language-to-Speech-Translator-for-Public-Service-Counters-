services:
  - type: web
    name: signbridge-api
    env: docker
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: ALLOWED_ORIGINS
        value: "*"
      - key: MODEL_DIR
        value: /app/models
